bugfix要从第一性原理出发 应该先从架构设计反思，而不是为了修复而修复

preference: 所有计划先写到文件里，再执行

开发准则

1. 编码前先思考
   动手实现前遵守以下要求：
   清晰写明所有前置假设；
   主动指出需求模糊点与方案取舍；
   若存在多种解读且会显著影响最终实现效果，必须先提问再编码；
   优先选择更简洁、合法可行的实现方案；
   禁止在需求缺失时自行脑补、悄悄补全逻辑。
2. 简洁优先
   只实现需求必需的最小代码；
   不预先开发暂无使用场景的功能、配置项；
   不为单一业务场景过度抽象封装；
   无需对理论上不可能出现的异常状态做防御处理；
   若代码实现冗余臃肿，及时简化重构。
3. 精准局部修改
   仅改动任务明确要求的文件与代码行；
   不重构无关业务代码；
   不格式化、排版无关文件；
   代码风格统一遵循仓库现有规范；
   仅删除本次改动产生的无用代码；
   遇到不相关 BUG 仅上报，无权私自修复。
   所有修改代码行，都必须能直接对应需求目标或配套测试用例。
4. 以目标为导向执行
   编码前先制定简短执行计划：
   复现或明确定义预期运行效果；
   新增 / 更新验证逻辑的测试用例；
   编写最小改动修复代码；
   执行针对性单元测试；
   根据改动风险范围执行全量校验。
   未完成验证前，不得标记任务完成。
   项目分层架构
   FastAPI 接口路由：存放于 src/api/
   业务逻辑层：存放于 src/services/
   数据库数据操作层：存放于 src/repositories/
   SQLAlchemy 数据库模型：存放于 src/models/
   PydanticAI 智能体相关逻辑：存放于 src/agent/
   Alembic 数据库迁移脚本：存放于 migrations/versions/
   硬性约束：路由层禁止直接操作持久化存储、编排智能体执行流程。
